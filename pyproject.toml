[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neurogolf"
version = "0.1.0"
description = "The 2026 NeuroGolf Championship competition workspace"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
