{
  "exp_id": "exp_b023_seddik_surgery_pattern_audit",
  "date": "2026-06-06",
  "status": "pattern_audit_ready",
  "notebook": "experiments\\exp_b022_public_notebook_intelligence\\notebooks\\seddiktrk_surgical_precision\\surgical-onnx-precision-parameter-reduction.ipynb",
  "code_cell_count": 20,
  "function_names": [
    "score_task",
    "cleanup_unused_initializers",
    "cleanup_all_tasks",
    "initializer_key",
    "find_duplicate_initializers",
    "deduplicate_initializers",
    "find_compressible_initializers",
    "compress_uniform_initializers"
  ],
  "import_counts": {
    "sys": 1,
    "neurogolf_utils": 1,
    "math": 1,
    "copy": 1,
    "tqdm.auto": 1,
    "collections": 1,
    "pandas": 1,
    "numpy": 1,
    "onnx": 2,
    "onnxruntime": 1,
    "zipfile": 1
  },
  "patterns": [
    {
      "pattern": "initializer_pruning",
      "evidence_count": 53,
      "useful_interpretation": "未使用initializerやinitializer型/shapeの削減は、exp023/b021系の安全なgraph surgeryに近い。",
      "exp_b_action": "strict seed全taskでunused initializer / duplicate initializer / scalar initializer化を再監査する。",
      "risk": "low-to-medium: validationとstatic check必須。"
    },
    {
      "pattern": "compress_or_banned_op_awareness",
      "evidence_count": 15,
      "useful_interpretation": "公開notebookにはbanned/危険opへの言及があり、strict filterのguardrailとして使える。",
      "exp_b_action": "candidate emission前にbanned op検出を強化し、Compress系artifactをsubmit candidateから外す。",
      "risk": "low: guardrail強化。"
    },
    {
      "pattern": "precision_parameter_reduction",
      "evidence_count": 56,
      "useful_interpretation": "parameter reductionやdtype削減はcostに効く可能性があるが、公式入力/出力float one-hotとの整合が必要。",
      "exp_b_action": "strict seedからinitializer dtype/constant size削減候補を生成し、full-arc gateで評価する。",
      "risk": "medium: dtype変更は数値/argmax挙動を壊す可能性。"
    },
    {
      "pattern": "score_cost_loop",
      "evidence_count": 45,
      "useful_interpretation": "score loopの構造はofficial-like profilingの再現に役立つ。",
      "exp_b_action": "exp_bのcandidate evaluationにprofile/cost failure reasonをより細かく記録する。",
      "risk": "low."
    },
    {
      "pattern": "pad_conv_where_patterns",
      "evidence_count": 3,
      "useful_interpretation": "Pad/Conv/Where/Slice/Gatherの使用頻度から、cheap loweringとheavy loweringの境界を読む。",
      "exp_b_action": "massimiliano/vyanktesh notebookと合わせてcheap Slice/Gather/Pad lowering patternを抽出する。",
      "risk": "medium: naive full-grid patternsはb011のように高cost化。"
    }
  ],
  "snippets": {
    "initializer_pruning": [
      "ath.basename(path).replace(\"task\",\"\").replace(\".onnx\",\"\"))\\n    model = onnx.load(path)\\n    \\n    used = set()\\n    for node in model.graph.node:\\n        used.update(inp for inp in node.input if inp)\\n    \\n    wasted = []\\n    for init in model.graph.initializer:\\n        if init.name not in used:\\n            dims = list(init.dims) if init.dims else [1]\\n            wasted.append((init.name, dims, int(np.prod(dims))))\\n    \\n    if wasted:\\n        total_wasted = sum(w[2] for w in wasted)\\n        total",
      "           wasted.append((init.name, dims, int(np.prod(dims))))\\n    \\n    if wasted:\\n        total_wasted = sum(w[2] for w in wasted)\\n        total_params = sum(int(np.prod(i.dims)) if i.dims else 1 \\n                          for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':",
      "                      for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':>8} {'Total':>8} {'% Waste':>8}   Unused tensors\")\\nprint(f\"{'─'*6} {'─'*8} {'─'*8} {'─'*8}   {'─'*30}\")\\nfor task_num, wasted, total, details in worst_tasks[:15]:\\n    pct = 100 * wasted / total",
      "               for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':>8} {'Total':>8} {'% Waste':>8}   Unused tensors\")\\nprint(f\"{'─'*6} {'─'*8} {'─'*8} {'─'*8}   {'─'*30}\")\\nfor task_num, wasted, total, details in worst_tasks[:15]:\\n    pct = 100 * wasted / total if total el",
      "_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':>8} {'Total':>8} {'% Waste':>8}   Unused tensors\")\\nprint(f\"{'─'*6} {'─'*8} {'─'*8} {'─'*8}   {'─'*30}\")\\nfor task_num, wasted, total, details in worst_tasks[:15]:\\n    pct = 100 * wasted / total if total else 0\\n    names = \", \".join(f\"'{d[0]}'\" for d in details[:3])\\n    if len(deta",
      "score_before, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE cleanup:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Memory:     {mem_before} bytes\")\\nprint(f\"  Score:      {score_before:.4f}\")\\nprint(f\"  Unused initializers:\")\\n\\nused = set()\\nfor node in network.graph.node:\\n    used.update(inp for inp in node.input if inp)\\n\\nfor init in network.graph.initializer:\\n    if init.name not in used:\\n        dims = list(init.dims) if init.dims else [1]\\n    ",
      "efore, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE cleanup:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Memory:     {mem_before} bytes\")\\nprint(f\"  Score:      {score_before:.4f}\")\\nprint(f\"  Unused initializers:\")\\n\\nused = set()\\nfor node in network.graph.node:\\n    used.update(inp for inp in node.input if inp)\\n\\nfor init in network.graph.initializer:\\n    if init.name not in used:\\n        dims = list(init.dims) if init.dims else [1]\\n        print(f\"",
      "}\")\\nprint(f\"  Memory:     {mem_before} bytes\")\\nprint(f\"  Score:      {score_before:.4f}\")\\nprint(f\"  Unused initializers:\")\\n\\nused = set()\\nfor node in network.graph.node:\\n    used.update(inp for inp in node.input if inp)\\n\\nfor init in network.graph.initializer:\\n    if init.name not in used:\\n        dims = list(init.dims) if init.dims else [1]\\n        print(f\"    ❌ '{init.name}' shape={dims} → {int(np.prod(dims))} wasted params\")\\n\\ncleaned = copy.deepcopy(network)\\n\\nremoved = []\\nfor init in list(cl",
      "if init.name not in used:\\n        dims = list(init.dims) if init.dims else [1]\\n        print(f\"    ❌ '{init.name}' shape={dims} → {int(np.prod(dims))} wasted params\")\\n\\ncleaned = copy.deepcopy(network)\\n\\nremoved = []\\nfor init in list(cleaned.graph.initializer):\\n    if init.name not in used:\\n        cleaned.graph.initializer.remove(init)\\n        removed.append(init.name)\\n\\nonnx.checker.check_model(cleaned)\\nscore_after, mem_after, params_after = score_task(t, cleaned)\\n\\n!rm *.json 2>/dev/null\\n!rm *",
      "ims else [1]\\n        print(f\"    ❌ '{init.name}' shape={dims} → {int(np.prod(dims))} wasted params\")\\n\\ncleaned = copy.deepcopy(network)\\n\\nremoved = []\\nfor init in list(cleaned.graph.initializer):\\n    if init.name not in used:\\n        cleaned.graph.initializer.remove(init)\\n        removed.append(init.name)\\n\\nonnx.checker.check_model(cleaned)\\nscore_after, mem_after, params_after = score_task(t, cleaned)\\n\\n!rm *.json 2>/dev/null\\n!rm *.onnx 2>/dev/null\\n\\nprint(f\"\\nTask {t:03d} AFTER cleanup:\")\\nprint(f"
    ],
    "compress_or_banned_op_awareness": [
      "moved\")\\nprint(f\"✅ Zero computation changes\")\\n\\nSAFE_OPS = {'Greater', 'Less', 'Equal', 'Add', 'Sub', 'Mul', 'Div',\\n            'Where', 'Max', 'Min', 'And', 'Or', 'Not', 'Clip',\\n            'LessOrEqual', 'GreaterOrEqual', 'Sum'}\\n\\n\\ndef find_compressible_initializers(model):\\n    \"\"\"Find initializers where all elements are identical.\"\"\"\\n    arrs = {}\\n    for init in model.graph.initializer:\\n        arrs[init.name] = numpy_helper.to_array(init)\\n    \\n    consumers = defaultdict(list)\\n    ",
      "candidates.append({\\n            \"name\": name, \"shape\": arr.shape, \"dtype\": arr.dtype,\\n            \"value\": flat[0].item(), \"saveable\": size - 1,\\n            \"consumers\": consumer_ops, \"safe\": is_safe,\\n        })\\n    return candidates\\n\\n\\ndef compress_uniform_initializers(model):\\n    \"\"\"Replace uniform-value initializers with scalars when all consumers broadcast safely.\"\"\"\\n    arrs = {}\\n    for init in model.graph.initializer:\\n        arrs[init.name] = numpy_helper.to_array(init)\\n    \\n ",
      "lar, init.name))\\n        total_saved += size - 1\\n    \\n    return model, total_saved\\n\\nsafe_total, unsafe_total = 0, 0\\nsafe_tasks = []\\n\\nfor t in tqdm(range(1, 401)):\\n    model = onnx.load(f\"submission/task{t:03d}.onnx\")\\n    candidates = find_compressible_initializers(model)\\n    \\n    task_safe = sum(c[\"saveable\"] for c in candidates if c[\"safe\"])\\n    task_unsafe = sum(c[\"saveable\"] for c in candidates if not c[\"safe\"])\\n    safe_total += task_safe\\n    unsafe_total += task_unsafe\\n    \\n   ",
      "afe_total += task_unsafe\\n    \\n    if task_safe > 0:\\n        safe_tasks.append((t, task_safe, [c for c in candidates if c[\"safe\"]]))\\n\\nsafe_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 Uniform tensors across 400 models:\\n\")\\nprint(f\"  ✅ Safe to compress (broadcast-friendly ops): {safe_total:>6d} params\")\\nprint(f\"  ⚠️  Unsafe (shape-dependent ops):             {unsafe_total:>6d} params\")\\nprint(f\"\\n  {len(safe_tasks)} tasks have safe compressible tensors\\n\")\\nprint(f\"{'Task':>6} {'Saveable':",
      "nsors across 400 models:\\n\")\\nprint(f\"  ✅ Safe to compress (broadcast-friendly ops): {safe_total:>6d} params\")\\nprint(f\"  ⚠️  Unsafe (shape-dependent ops):             {unsafe_total:>6d} params\")\\nprint(f\"\\n  {len(safe_tasks)} tasks have safe compressible tensors\\n\")\\nprint(f\"{'Task':>6} {'Saveable':>9}   Example tensor\")\\nprint(f\"{'─'*6} {'─'*9}   {'─'*45}\")\\nfor task_num, saved, cands in safe_tasks[:15]:\\n    c = cands[0]\\n    print(f\"  {task_num:03d}    {saved:>6}   '{c['name']}' {c['shap",
      "}   '{c['name']}' {c['shape']} val={c['value']} → scalar\")\\n\\n# Pick the worst offender\\nt = 285\\nnetwork = onnx.load(f\"submission/task{t:03d}.onnx\")\\n\\nscore_before, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE compression:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Score:      {score_before:.4f}\")\\n\\ncands = safe_tasks[0][2]\\nprint(f\"  Compressible tensors: {len(cands)}\")\\nfor c in cands[:5]:\\n    ops = \", \".join(c[\"consumers\"])\\n    print(f\"    '{c['",
      "03d}.onnx\")\\n\\nscore_before, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE compression:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Score:      {score_before:.4f}\")\\n\\ncands = safe_tasks[0][2]\\nprint(f\"  Compressible tensors: {len(cands)}\")\\nfor c in cands[:5]:\\n    ops = \", \".join(c[\"consumers\"])\\n    print(f\"    '{c['name']}' {c['shape']} val={c['value']} → saves {c['saveable']} params  [{ops}]\")\\nif len(cands) > 5:\\n    print(f\"    ... +{len(cands)-5",
      "oin(c[\"consumers\"])\\n    print(f\"    '{c['name']}' {c['shape']} val={c['value']} → saves {c['saveable']} params  [{ops}]\")\\nif len(cands) > 5:\\n    print(f\"    ... +{len(cands)-5} more\")\\n\\n\\noptimized = copy.deepcopy(network)\\noptimized, saved = compress_uniform_initializers(optimized)\\nonnx.checker.check_model(optimized)\\n\\nscore_after, mem_after, params_after = score_task(t, optimized)\\n\\n!rm *.json 2>/dev/null\\n!rm *.onnx 2>/dev/null\\n\\nprint(f\"\\nTask {t:03d} AFTER compression:\")\\nprint(f\"  Para",
      ")\\noptimized, saved = compress_uniform_initializers(optimized)\\nonnx.checker.check_model(optimized)\\n\\nscore_after, mem_after, params_after = score_task(t, optimized)\\n\\n!rm *.json 2>/dev/null\\n!rm *.onnx 2>/dev/null\\n\\nprint(f\"\\nTask {t:03d} AFTER compression:\")\\nprint(f\"  Parameters: {params_before} → {params_after}  (saved {params_before - params_after})\")\\nprint(f\"  Score:      {score_before:.4f} → {score_after:.4f}  (Δ{score_after - score_before:+.4f})\")\\nprint(f\"\\n✅ Same computation. Same ",
      "{params_before} → {params_after}  (saved {params_before - params_after})\")\\nprint(f\"  Score:      {score_before:.4f} → {score_after:.4f}  (Δ{score_after - score_before:+.4f})\")\\nprint(f\"\\n✅ Same computation. Same outputs. Higher score.\")\\n\\n\"\"\"Compress uniform initializers to scalars across all tasks.\"\"\"\\n\\nSKIP_TASKS = {158}  # Known to fail on grader despite passing locally\\n\\ntask_dir=\"/kaggle/working/submission\"\\ntask_paths = sorted(glob.glob(f\"{task_dir}/task*.onnx\"))\\ntotal_saved = 0\\ntas"
    ],
    "precision_parameter_reduction": [
      "m\\nfrom collections import defaultdict, Counter\\n\\n\\nimport pandas as pd\\nimport numpy as np\\n\\n\\nimport onnx\\nfrom onnx import helper, TensorProto, numpy_helper\\nimport onnxruntime\\n\\ndef score_task(task_num, network):\\n    \"\"\"Returns (points, memory, params) or None if failed.\"\"\"\\n    filename = f\"task{task_num:03d}.onnx\"\\n    onnx.save(network, filename)\\n    if not check_network(filename):\\n        return None\\n\\n    try:\\n        sanitized = onnx.load(filename)\\n        for node in sanitized.graph",
      "except Exception:\\n        return None\\n\\n    examples = load_examples(task_num)\\n    agi_r, agi_w, _ = verify_subset(session, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w, _ = verify_subset(session, examples[\"arc-gen\"])\\n\\n    memory, params = score_network(sanitized, session.end_profiling())\\n    if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log",
      "i_r, agi_w, _ = verify_subset(session, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w, _ = verify_subset(session, examples[\"arc-gen\"])\\n\\n    memory, params = score_network(sanitized, session.end_profiling())\\n    if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log(max(1.0, memory + params)))\\n    return points, memory, params\\n\\nworst_tasks = []\\n\\nfo",
      "ssion, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w, _ = verify_subset(session, examples[\"arc-gen\"])\\n\\n    memory, params = score_network(sanitized, session.end_profiling())\\n    if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log(max(1.0, memory + params)))\\n    return points, memory, params\\n\\nworst_tasks = []\\n\\nfor path in tqdm(sorted(glob.glob(",
      "nitized, session.end_profiling())\\n    if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log(max(1.0, memory + params)))\\n    return points, memory, params\\n\\nworst_tasks = []\\n\\nfor path in tqdm(sorted(glob.glob(\"submission/task*.onnx\"))):\\n    task_num = int(os.path.basename(path).replace(\"task\",\"\").replace(\".onnx\",\"\"))\\n    model = onnx.load(path)\\n    \\n    us",
      " if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log(max(1.0, memory + params)))\\n    return points, memory, params\\n\\nworst_tasks = []\\n\\nfor path in tqdm(sorted(glob.glob(\"submission/task*.onnx\"))):\\n    task_num = int(os.path.basename(path).replace(\"task\",\"\").replace(\".onnx\",\"\"))\\n    model = onnx.load(path)\\n    \\n    used = set()\\n    for node in model.grap",
      "\\n        if init.name not in used:\\n            dims = list(init.dims) if init.dims else [1]\\n            wasted.append((init.name, dims, int(np.prod(dims))))\\n    \\n    if wasted:\\n        total_wasted = sum(w[2] for w in wasted)\\n        total_params = sum(int(np.prod(i.dims)) if i.dims else 1 \\n                          for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x",
      "asted:\\n        total_wasted = sum(w[2] for w in wasted)\\n        total_params = sum(int(np.prod(i.dims)) if i.dims else 1 \\n                          for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':>8} {'Total':>8} {'% Waste':>8}   Unused tensors\")\\nprint(f\"{'─'*6}",
      "or w in wasted)\\n        total_params = sum(int(np.prod(i.dims)) if i.dims else 1 \\n                          for i in model.graph.initializer)\\n        worst_tasks.append((task_num, total_wasted, total_params, wasted))\\n\\n# Sort by most wasted params\\nworst_tasks.sort(key=lambda x: -x[1])\\n\\nprint(f\"📊 {len(worst_tasks)} tasks have unused initializers\\n\")\\nprint(f\"{'Task':>6} {'Wasted':>8} {'Total':>8} {'% Waste':>8}   Unused tensors\")\\nprint(f\"{'─'*6} {'─'*8} {'─'*8} {'─'*8}   {'─'*30}\")\\nfo",
      " += f\" +{len(details)-3} more\"\\n    print(f\"  {task_num:03d}    {wasted:>6}   {total:>6}   {pct:>5.1f}%   {names}\")\\n\\n# 🔬 Let's look at Task XXX in detail\\nt = 285\\nnetwork = onnx.load(f\"submission/task{t:03d}.onnx\")\\n\\nscore_before, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE cleanup:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Memory:     {mem_before} bytes\")\\nprint(f\"  Score:      {score_before:.4f}\")\\nprint(f\"  Unused initializers:\")\\n\\nused = s"
    ],
    "score_cost_loop": [
      "!pip install -q numpy==2.4.4 2>/dev/null\\n!pip install -q onnx==1.21.0 2>/dev/null\\n!pip install -q onnxruntime==1.24.4 2>/dev/null\\n!pip install -q onnx-tool==1.0.1 2>/dev/null\\n\\n!rm -r /kaggle/working/submission 2>/dev/null\\n!cp /kaggle/input/notebooks/massimilianoghiotto/convolution-series-part-4/submission.zip /kaggle/working/submission.zip\\n!unzip ",
      "ort *\\nshow_legend()\\n\\nimport math, glob, os\\nimport copy\\nfrom tqdm.auto import tqdm\\nfrom collections import defaultdict, Counter\\n\\n\\nimport pandas as pd\\nimport numpy as np\\n\\n\\nimport onnx\\nfrom onnx import helper, TensorProto, numpy_helper\\nimport onnxruntime\\n\\ndef score_task(task_num, network):\\n    \"\"\"Returns (points, memory, params) or None if failed.\"\"\"\\n    filename = f\"task{task_num:03d}.onnx\"\\n    onnx.save(network, filename)\\n    if not check_network(filename):\\n        return None\\n\\n    try:\\n",
      "()\\n\\nimport math, glob, os\\nimport copy\\nfrom tqdm.auto import tqdm\\nfrom collections import defaultdict, Counter\\n\\n\\nimport pandas as pd\\nimport numpy as np\\n\\n\\nimport onnx\\nfrom onnx import helper, TensorProto, numpy_helper\\nimport onnxruntime\\n\\ndef score_task(task_num, network):\\n    \"\"\"Returns (points, memory, params) or None if failed.\"\"\"\\n    filename = f\"task{task_num:03d}.onnx\"\\n    onnx.save(network, filename)\\n    if not check_network(filename):\\n        return None\\n\\n    try:\\n        san",
      "        return None\\n\\n    try:\\n        sanitized = onnx.load(filename)\\n        for node in sanitized.graph.node:\\n            node.name = node.output[0]\\n            if \"kernel_time\" in node.name:\\n                return None\\n        options = onnxruntime.SessionOptions()\\n        options.enable_profiling = True\\n        options.graph_optimization_level = onnxruntime.GraphOptimizationLevel.ORT_DISABLE_ALL\\n        options.profile_file_prefix = f\"{task_num:03}\"\\n        session = onnxruntime.Inf",
      "            node.name = node.output[0]\\n            if \"kernel_time\" in node.name:\\n                return None\\n        options = onnxruntime.SessionOptions()\\n        options.enable_profiling = True\\n        options.graph_optimization_level = onnxruntime.GraphOptimizationLevel.ORT_DISABLE_ALL\\n        options.profile_file_prefix = f\"{task_num:03}\"\\n        session = onnxruntime.InferenceSession(sanitized.SerializeToString(), options)\\n    except Exception:\\n        return None\\n\\n    examples = ",
      " in node.name:\\n                return None\\n        options = onnxruntime.SessionOptions()\\n        options.enable_profiling = True\\n        options.graph_optimization_level = onnxruntime.GraphOptimizationLevel.ORT_DISABLE_ALL\\n        options.profile_file_prefix = f\"{task_num:03}\"\\n        session = onnxruntime.InferenceSession(sanitized.SerializeToString(), options)\\n    except Exception:\\n        return None\\n\\n    examples = load_examples(task_num)\\n    agi_r, agi_w, _ = verify_subset(ses",
      "s = onnxruntime.SessionOptions()\\n        options.enable_profiling = True\\n        options.graph_optimization_level = onnxruntime.GraphOptimizationLevel.ORT_DISABLE_ALL\\n        options.profile_file_prefix = f\"{task_num:03}\"\\n        session = onnxruntime.InferenceSession(sanitized.SerializeToString(), options)\\n    except Exception:\\n        return None\\n\\n    examples = load_examples(task_num)\\n    agi_r, agi_w, _ = verify_subset(session, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w,",
      "ime.SessionOptions()\\n        options.enable_profiling = True\\n        options.graph_optimization_level = onnxruntime.GraphOptimizationLevel.ORT_DISABLE_ALL\\n        options.profile_file_prefix = f\"{task_num:03}\"\\n        session = onnxruntime.InferenceSession(sanitized.SerializeToString(), options)\\n    except Exception:\\n        return None\\n\\n    examples = load_examples(task_num)\\n    agi_r, agi_w, _ = verify_subset(session, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w, _ = verify_subse",
      "ception:\\n        return None\\n\\n    examples = load_examples(task_num)\\n    agi_r, agi_w, _ = verify_subset(session, examples[\"train\"] + examples[\"test\"])\\n    gen_r, gen_w, _ = verify_subset(session, examples[\"arc-gen\"])\\n\\n    memory, params = score_network(sanitized, session.end_profiling())\\n    if memory is None or params is None or memory < 0 or params < 0:\\n        return None\\n\\n    if agi_w + gen_w > 0:\\n        return None  # incorrect\\n\\n    points = max(1.0, 25.0 - math.log(max(1.0",
      "etails) > 3:\\n        names += f\" +{len(details)-3} more\"\\n    print(f\"  {task_num:03d}    {wasted:>6}   {total:>6}   {pct:>5.1f}%   {names}\")\\n\\n# 🔬 Let's look at Task XXX in detail\\nt = 285\\nnetwork = onnx.load(f\"submission/task{t:03d}.onnx\")\\n\\nscore_before, mem_before, params_before = score_task(t, network)\\nprint(f\"Task {t:03d} BEFORE cleanup:\")\\nprint(f\"  Parameters: {params_before}\")\\nprint(f\"  Memory:     {mem_before} bytes\")\\nprint(f\"  Score:      {score_before:.4f}\")\\nprint(f\"  Unuse"
    ],
    "pad_conv_where_patterns": [
      ">/dev/null\\n!pip install -q onnx==1.21.0 2>/dev/null\\n!pip install -q onnxruntime==1.24.4 2>/dev/null\\n!pip install -q onnx-tool==1.0.1 2>/dev/null\\n\\n!rm -r /kaggle/working/submission 2>/dev/null\\n!cp /kaggle/input/notebooks/massimilianoghiotto/convolution-series-part-4/submission.zip /kaggle/working/submission.zip\\n!unzip -q /kaggle/working/submission.zip -d /kaggle/working/submission\\n!rm /kaggle/working/submission.zip \\n!ls -la\\n\\nimport sys\\nsys.path.append(\"/kaggle/input/competitions/n",
      "ms\")\\n\\nprint(f\"\\n{'='*50}\")\\nprint(f\"🔗 {tasks_deduped} tasks deduplicated\")\\nprint(f\"📉 {total_saved} total params removed\")\\nprint(f\"✅ Zero computation changes\")\\n\\nSAFE_OPS = {'Greater', 'Less', 'Equal', 'Add', 'Sub', 'Mul', 'Div',\\n            'Where', 'Max', 'Min', 'And', 'Or', 'Not', 'Clip',\\n            'LessOrEqual', 'GreaterOrEqual', 'Sum'}\\n\\n\\ndef find_compressible_initializers(model):\\n    \"\"\"Find initializers where all elements are identical.\"\"\"\\n    arrs = {}\\n    for init in model.",
      "Greater', 'Less', 'Equal', 'Add', 'Sub', 'Mul', 'Div',\\n            'Where', 'Max', 'Min', 'And', 'Or', 'Not', 'Clip',\\n            'LessOrEqual', 'GreaterOrEqual', 'Sum'}\\n\\n\\ndef find_compressible_initializers(model):\\n    \"\"\"Find initializers where all elements are identical.\"\"\"\\n    arrs = {}\\n    for init in model.graph.initializer:\\n        arrs[init.name] = numpy_helper.to_array(init)\\n    \\n    consumers = defaultdict(list)\\n    for node in model.graph.node:\\n        for i, name in enu"
    ]
  },
  "decision": "Prioritize initializer/parameter surgery audit on strict seed, then dtype/constant-size reductions only with full-arc validation.",
  "local_estimate_delta": 0.0,
  "submission_decision": "no_submit: intelligence audit only",
  "leakage_risk": "medium: public notebook intelligence; use only as pattern hints.",
  "overfitting_risk": "low if used as guardrails; medium if copied as artifacts."
}