# 2026-06-10 Daily Log

## exp135_exp127_submission_confirmation

- `docs/experiment_plan_2026-06-10.md` に沿って Phase 0 を確認。
- exp127 はすでに Kaggle ref `53480507` として提出済みで、public LB は `5930.55`。
- exp_b025 から LB `+0.15`、local `+0.153` で、micro delta はほぼ 1:1 転写。
- local/LB gap は約 `-352.42` のまま。private functional failure task 群を特定する Phase A を次優先にする。

## Next

- exp136: strict/working seed の stress validation 候補 task 抽出。Done。
- exp144: dtype / memory cost probe。

## exp136_private_failure_subset_inventory

- Phase A-2 として、exp_b025 の per-task score から `-352.412218` gap を説明する failure subset を探索。
- 400 task inventory と 50 subset 候補を作成。
- best subset は 24 task で score sum `352.41`、gap error `0.0`。
- best subset tasks: `002 004 008 009 013 014 016 018 019 021 023 024 025 029 031 032 034 036 046 047 049 050 051 366`
- 判断: private functional failure 仮説を補強。ただし直接証拠ではないため、次は consensus/provenance/shape 外挿監査で bisection probe 前の候補を絞る。

## exp137_private_failure_consensus_audit

- exp136上位20 subsetから task frequency と採用モデルgraph構造を監査。
- unique candidate task: `56`
- top consensus:
  - task013 / task002 / task029 / task009: top20 freq `20/20`, exact-gap freq `4/4`
  - task024: top20 freq `19/20`
  - task018: top20 freq `18/20`
- 判断: bisection提出の前に task013/002/029/009/024/018 の provenance / 代替source / shape外挿リスクを監査する。

## exp138_priority_task_alternative_source_audit

- task013/002/029/009/024/018 の既存manifest/candidate_evalを横断。
- manifest rows `203`, candidate_eval rows `199`。
- 複数sourceは見つかったが、best alternative は同costの `exp004_public_blend_relaxed_static` 系で、実質的なrepair候補ではなさそう。
- 改善candidateはなし。
- 判断: top4 `013/002/029/009` の bisection fail-stub probe を作り、現public scoringで生きているかを直接測る。

## exp139_top4_failure_bisection_probe

- exp127 best zipをベースに、task013/002/029/009を fail stub へ差し替え。
- Kaggle ref `53520609`。
- public LB `5876.49`。
- exp127 LB `5930.55` からのdropは `54.06`。
- 4 taskが全てpublic scoringで生きていた場合の期待drop `54.05675` と一致。
- 判断: task013/002/029/009 はgap原因の「既に0点task」ではない。次はこれらを除外し、次点groupをprobe/監査する。

## exp140_next4_failure_bisection_probe

- exp127 best zipをベースに、task024/018/004/019を fail stub へ差し替え。
- Kaggle ref `53520700`。
- public LB `5886.89`。
- exp127 LB `5930.55` からのdropは `43.66`。
- 4 task all-alive期待dropは `57.01524`。
- 差分 `13.355` は task018 の点数 `13.35099` と一致。
- 判断: task024/004/019 はpublic alive、task018 はpublic 0点の可能性が非常に高い。task018 repairを最優先にする。

## exp141-exp143 task018 repair probes

- exp141: exp023 teacher artifactは cost `17467` だが `24_pass_1_fail` で提出停止。
- exp142: task018既存candidateを監査。unique raw `6`、full-okは strict / beicicc / exp_b035。
- exp143: beicicc full-ok候補を単独提出。ref `53520866`、LB `5930.55`。
- 判断: beiciccもpublic-zero。採用しない。残る既存full-ok別rawは exp_b035。

## exp144_task018_b035_repair_probe

- task018の残るfull-ok別raw `exp_b035_new_source_full_arc_blend` を単独差し替え。
- local validation `266_pass_0_fail`。
- Kaggle ref `53520958`。
- public LB `5942.48`。
- exp127 `5930.55` から `+11.93`。
- 判断: task018 public-zero repair成功。current public LB best。ただしsource/private robustness riskはhigh。

## exp145_next4b_failure_bisection_probe

- task032/050/016/031をfail-stub probe。
- Kaggle ref `53521106`。
- public LB `5859.97`。
- exp127からのdrop `70.58` が all-alive期待 `70.57623` と一致。
- 判断: task032/050/016/031 はpublic-scoring alive。public-zero suspectから除外。

## exp146_next4c_failure_bisection_probe

- task021/014/025/008をfail-stub probe。
- Kaggle ref `53521220`。
- public LB `5887.02`。
- exp127からのdrop `43.53`、all-alive期待 `57.12815`。
- 不足分 `13.598` は task025 points `13.60040` と一致。
- 判断: task025 はpublic-zeroの可能性が非常に高い。次repair target。

## exp147_task025_candidate_validation_audit

- task025既存source候補を監査。
- source rows `34`, unique raw `5`, full-ok `1`。
- full-okは現行 `franksunp_blended_best` rawのみで、これはpublic-zero。
- 判断: task025は既存source差し替えでは修復できない。rule repair / robust loweringが必要。短期では次のpublic-zero探索を続ける。

## exp148_next4d_failure_bisection_probe

- task366/077/064/084をfail-stub probe。
- Kaggle ref `53521453`。
- public LB `5877.68`。
- exp127からのdrop `52.87` は all-alive期待 `52.78042` とほぼ一致。
- 判断: task366/077/064/084 は大きなpublic-zero evidenceなし。短期suspectから外し、未probe候補を続けて探索する。

## exp149_next4e_failure_bisection_probe

- task053/056/067/005をfail-stub probe。
- Kaggle ref `53521652`。
- public LB `5856.74`。
- exp127からのdrop `73.81` は all-alive期待 `73.81309` と一致。
- 判断: task053/056/067/005 はpublic-zero evidenceなし。次は残り頻出候補へ進む。

## exp150_next4f_failure_bisection_probe

- task023/036/085/034をfail-stub probe。
- Kaggle ref `53521805`。
- public LB `5890.05`。
- exp127からのdrop `40.50` に対し、all-alive期待dropは `55.52734`。
- 不足分 `15.0273` は task023 points `15.02731` と一致。
- 判断: task023 はpublic-zeroの可能性が非常に高い。次repair target。task036/085/034 はpublic alive扱い。

## exp151_task023_candidate_validation_audit

- task023既存source候補を監査。
- source rows `33`, unique raw `5`, full-ok `2`。
- full-okは現行 `franksunp_blended_best` rawと、別raw `exp_b035_new_source_full_arc_blend`。
- `exp_b035` rawは cost `72612`, validation `266_pass_0_fail`。
- 判断: exp144 bestにtask023 exp_b035 rawを重ねる単独repair probeへ進む。

## exp152_task023_b035_repair_probe

- exp144 bestにtask023 `exp_b035` rawを差し替え。
- Kaggle ref `53521952`。
- public LB `5956.28`。
- exp144比 `+13.80`、exp127比 `+25.73`。
- 判断: current public LB bestを更新。ただしtask018/task023ともにpublic/source blend由来repairのため、private robustness riskはhigh。

## exp153_next4g_failure_bisection_probe

- task051/049/046/011をfail-stub probe。
- Kaggle ref `53522084`。
- public LB `5873.53`。
- exp127からのdrop `57.02` は all-alive期待 `57.02309` と一致。
- 判断: task051/049/046/011 はpublic-zero evidenceなし。短期suspectから外す。

## exp154_next4h_failure_bisection_probe

- task033/087/276/030をfail-stub probe。
- Kaggle ref `53522207`。
- public LB `5859.07`。
- exp127からのdrop `71.48` は all-alive期待 `71.47883` と一致。
- 判断: task033/087/276/030 はpublic-zero evidenceなし。短期suspectから外す。

## exp155_next4i_failure_bisection_probe

- task063/022/037/020をfail-stub probe。
- Kaggle ref `53522390`。
- public LB `5873.60`。
- exp127からのdrop `56.95` は all-alive期待 `56.94773` と一致。
- 判断: task063/022/037/020 はpublic-zero evidenceなし。短期suspectから外す。

## exp156_next4j_failure_bisection_probe

- task027/082/088/092をfail-stub probe。
- Kaggle ref `53522565`。
- public LB `5873.06`。
- exp127からのdrop `57.49` は all-alive期待 `57.48547` と一致。
- 判断: task027/082/088/092 はpublic-zero evidenceなし。短期suspectから外す。
- 採点待ち中に次候補 `090/173/028/091` の exp157 zip を準備した。

## exp157_next4k_failure_bisection_probe

- task090/173/028/091をfail-stub probe。
- Kaggle ref `53522643`。
- public LB `5873.63`。
- exp127からのdrop `56.92` は all-alive期待 `56.91703` と一致。
- 判断: task090/173/028/091 はpublic-zero evidenceなし。subset頻出候補は薄くなったため、追加bisectionだけでなく task025 repair / dtype cost lane への切替を検討する。

## exp158_dtype_memory_cost_probe

- Phase C-1 dtype/memory cost probeを実行。
- fp32 full-grid中間: cost `36000`。
- fp16 roundtrip: cost `18000`。
- bool/uint8 roundtrip: cost `9000`。
- int32 roundtrip: cost `36000`。
- 判断: 公式costはdtype幅を反映している。targeted boolean/uint8 mask post-passは有望。ただし演算数が増えると中間が増えるため、広いfp16化ではなくtask/graph patternを絞って試す。

## exp159_dtype_postpass_candidate_audit

- exp152 current best bundle 400 taskをdtype post-pass観点で構造監査。
- top heuristic candidates: task366 / task284 / task158 / task382 / task187。
- task158は full-grid boolish-to-FLOAT Cast が `18` 個あり、局所rewrite監査の最初の対象にしやすい。
- 判断: 次はtask158のCast consumer patternを確認し、semantic-preservingにFLOAT materializationを減らせるかを見る。

## exp160_task158_dtype_rewrite_inspection

- task158のfull-grid boolish-to-FLOAT Cast `18` 個を監査。
- すべてのCast出力は `Sum` に入っており、boolean maskの数値カウント用途だった。
- 判断: task158の直接dtype removalは安全ではなさそう。次はWhere条件/mask selectionに近い候補をinspectする。

## exp161_where_dtype_rewrite_inspection

- task206/338/328/366のWhere周辺dtype patternを監査。
- task206/328の削れそうなCastは最終 `output` へのFLOAT Castで、公式interface上削除不可。
- task338/366はすでにUINT8/FLOAT16/BOOL branchを多用している。
- 判断: 既存artifactへのbroad dtype post-passは即効性が薄い。dtype laneは新規lowering設計時のcost-aware表現として使う。

## exp162_task025_rule_diagnostic

- exp146/147でpublic-zeroかつ既存source差し替え不可と判明したtask025について、rule repair入口の差分診断を実行。
- 266例すべてshape preservedで、出力色はすべて入力既存色に含まれる。
- ただし `all_changes_on_zero_count=0` で純粋なzero-fillではない。
- identity / most-common fill / row-col fill / bbox fill / mirror / rot180 はすべて `0/266`。
- 判断: task025は単純fill/幾何変換ではなく、既存色セルの sparse relocation/recolor 型に見える。次はchanged-cell target predicateと色移動構造を採掘する。

## exp163_task025_motion_predicate_mining

- task025の差分を色別の削除/追加ペアとして診断。
- color/nonzero count preserved は `64/266` のみで、多くは余剰stray cellを削除する。
- dominant line type は horizontal `139`、vertical `117`。
- 判断: 任意recolorではなく、縦/横guide lineへのaxis-aligned projectionが有力。

## exp164_task025_line_projection_rule

- task025について、完全な縦/横ラインをguideとして、同色stray cellをline隣接セルへ射影するinput-only ruleを検証。
- Python reference が `266/266` full pass。
- rule: guide lineを持つ色だけを残し、off-line同色セルを同じ行/列のline隣接側へ移動。guide lineを持たない色は削除。
- 判断: task025はrule-found。次はONNX loweringとcost validationを行い、current public-zero model cost `89286` を置換できるか確認する。

## exp165_task025_line_projection_onnx_probe

- exp164のguide-line projection ruleをcorrectness-first ONNXへlowering。
- full validation: `266_pass_0_fail`。
- candidate cost: `491600`、baseline cost: `89286`。
- 判断: ruleのONNX表現可能性は確認できたが、9色それぞれにfull-grid reduction / MatMul / bool maskを展開する実装は重すぎる。採用・提出しない。次は色loopの圧縮とfull-grid中間削減を狙う。

## exp166_task025_line_projection_publiczero_repair

- exp165はlocal costではno gainだが、task025はpublic-zeroなので `0 -> 11.89458` 点のrepair候補として扱う。
- exp152 current public bestにtask025 line-projection candidateを重ねたzipを作成。
- candidate validation: `266_pass_0_fail`。
- zip sanity: 400 files / names OK / sha256 `8468ede68913b54bfd3a75a953ac64c122dd5e4b13017aa4b83447387e720193`。
- Kaggle ref `53523413` として提出。expected public LB if pass: `5968.17`。
- public LB: `5968.18`。
- 判断: task025 public-zero repair成功。exp152 `5956.28` から `+11.90` で、new current public LB best。

## exp167_next4l_failure_bisection_probe

- exp166 current bestをベースに、残りsubset頻度上位の task048/035/012/017 をfail-stub化。
- expected_drop_if_all_alive: `61.82369`。
- expected_lb_if_all_alive: `5906.36`。
- zip sanity: 400 files / names OK / sha256 `ce1c4148006ffb73aeaa67e651f40c2df476ab53dd3c081c0c5f4269a3f50c85`。
- Kaggle ref `53523591` として提出。
- public LB: `5906.33`。
- 判断: all-alive期待 `5906.36` と一致。task048/035/012/017 はpublic-zeroではないため、短期repair suspectから除外。

## exp168_next4l_candidate_validation_audit

- exp167採点待ち中に、task048/035/012/017の既存source候補を監査。
- full_ok_total: `9`。
- task048/035/012/017すべてにfull-local-valid既存候補あり。
- ただしexp167で4 task all-aliveと判明したため、即repairではなく将来のcost/source分析用に保持。

## exp169_next4m_failure_bisection_probe

- exp166 current bestをベースに、task133/396/158/047をfail-stub化して提出。
- Kaggle ref `53523802`、public LB `5938.07`。
- all-alive expected LB `5912.36` に対して `+25.71` 高く、これは `task133 + task158` の点数と一致。
- 判断: task133/task158はpublic-zero疑い。task396/task047はpublic-scoring aliveとして短期repair suspectから除外。

## exp170_next4m_candidate_validation_audit

- exp169採点待ち中に、task133/396/158/047の既存source候補を監査。
- full_ok_total: `8`。
- task133/task158にdistinct exp_b035 full-local-valid候補あり。
- task396は実質current-only、task047は同一raw重複。

## exp171_next4n_failure_bisection_probe

- exp169が全aliveだった場合に備え、task003/038/001/086のfail-stub probe zipを事前準備。
- expected_lb_if_all_alive: `5903.10`。
- zip sanity pass、4 target stubはすべて `0_pass_1_fail`。
- exp169/exp172が先にscore-producingになったため、提出は保留。

## exp172_task133_158_b035_repair_probe

- exp169でpublic-zeroと推定したtask133/task158を、exp_b035 full-local-valid rawへ差し替え。
- validation: task133 `267_pass_0_fail`, task158 `266_pass_0_fail`。
- Kaggle ref `53523884`、public LB `5993.82`。
- exp166 `5968.18` から `+25.64`。期待 `5993.8188` と一致し、new current public LB best。
- リスク: exp_b035 public/source blend由来なのでprivate robustness riskは高い。最終候補扱いにはrisk reviewが必要。

## exp173_next4n_candidate_validation_audit

- exp171候補task003/038/001/086の既存source候補を監査。
- full_ok_total: `8`。
- task001にはdistinct beicicc候補、task086にはaccepted surgery variantsあり。
- task003/task038は明確なdistinct repair sourceが少ない。

## exp171_next4n_failure_bisection_probe update

- exp172 current bestをベースにtask003/038/001/086をfail-stub化して提出。
- Kaggle ref `53524036`、public LB `5928.74`。
- all-alive expected `5928.7422` と一致。
- 判断: task003/038/001/086はpublic-scoring alive。短期public-zero suspectから除外。

## exp174_next2o_failure_bisection_probe

- subset由来候補で残ったtask285/286をfail-stub化して提出。
- Kaggle ref `53524145`、public LB `5980.81`。
- exp172からのobserved dropは `13.01` でtask286 alive分に一致。missing drop `13.06` はtask285点数と一致。
- 判断: task285はpublic-zero、task286はalive。

## exp175_next2o_candidate_validation_audit

- task285/286の既存source候補を監査。
- full_ok_total: `4`。
- task285にはexp_b035 distinct full-local-valid rawあり。ただしcost `395468` で点数は `12.112175` に低下。
- task286はcurrent系以外の有効repair候補が薄い。

## exp176_risk_top4_failure_bisection_probe

- subset候補が薄くなった後に備え、risk_score上位未probeのtask202/382/205/383をfail-stub化するzipを準備。
- expected_lb_if_all_alive: `5940.53`。
- exp178結果後の次probe候補として保持。

## exp177_risk_top4_candidate_validation_audit

- exp176対象task202/382/205/383の既存source候補を監査。
- full_ok_total: `8`。
- task383はdistinct repair surfaceが比較的あり、task202/382は同一raw寄り、task205はcurrent-only。

## exp178_task285_b035_repair_probe

- exp174でpublic-zeroと推定したtask285をexp_b035 full-local-valid rawへ差し替え。
- validation: `265_pass_0_fail`、cost `395468`。
- Kaggle ref `53524248`、public LB `6005.93`。
- exp172 `5993.82` から `+12.11`。期待 `6005.9322` と一致し、new current public LB best。6000を突破。
- リスク: exp_b035 public/source blend由来なのでprivate robustness riskは高い。

## exp176/179/181/182 risk-inventory failure probes

- exp178 current bestをベースに、risk inventory上位の未probe taskを4件ずつfail-stub化。
- exp176 task202/382/205/383: ref `53526598`, public LB `5952.64`, all-alive一致。
- exp179 task251/109/239/358: ref `53526669`, public LB `5952.01`, all-alive一致。
- exp181 task281/203/126/159: ref `53526726`, public LB `5951.48`, all-alive一致。
- exp182 task313/370/234/303: ref `53526782`, public LB `5951.46`, all-alive一致。
- 判断: 16 taskを短期public-zero suspectから除外。exp183 task187/204/198/364を次probeとして提出済み。

## exp183/185/186 task187 public-zero repair

- exp183 task187/204/198/364 fail-stub probe: ref `53526873`, public LB `5965.28`。
- all-alive expected `5951.8651` より `+13.41` 高く、missing dropはtask187 points `13.4353` と一致。
- exp185でtask187既存候補を監査し、現bestとは別rawの `massimilianoghiotto_6254` full-local-valid候補を確認: `266_pass_0_fail`, cost `105313`。
- exp186としてexp178 + task187 repair zipを作成し提出。expected LB `6019.37`。
- 結果: ref `53526998`, public LB `6005.90`。exp178 `6005.93` を更新せず、採用しない。
- 判断: task187は未解決public-zero repair queueに残す。この既存source rawは公開repairにならないため、次は別候補またはrule repairが必要。

## exp184/187 next risk probe preparation

- exp184 task284/300/379/340 fail-stub probeを作成し提出。
- expected_lb_if_all_alive: `5951.0653`。
- 採点待ちの間にexp187 candidate auditを実施。4 taskすべてにfull-local-valid既存候補があり、当たりが出た場合はrepair候補を即選定可能。
- 結果: ref `53527058`, public LB `5951.06`。all-alive expectedと一致。
- 判断: task284/300/379/340は短期public-zero suspectから除外。exp188 task328/301/306/387を次probeとして提出。

## exp188/189 next risk probe

- exp188 task328/301/306/387 fail-stub probeを提出。
- 結果: ref `53527133`, public LB `5950.27`。all-alive expected `5950.2733` と一致。
- 判断: task328/301/306/387は短期public-zero suspectから除外。
- 採点待ち中にexp189 candidate auditを実施。5 full-local-valid候補を確認したが、exp188 all-aliveのため即repairには使わない。
- exp190 task238/112/377/177を次probeとして提出。

## exp190/191 next risk probe

- exp190 task238/112/377/177 fail-stub probeを提出。
- 結果: ref `53527204`, public LB `5950.05`。all-alive expected `5950.0478` と一致。
- 判断: task238/112/377/177は短期public-zero suspectから除外。
- exp191 candidate auditはfull_ok_total `6` だったが、exp190 all-aliveのため即repairには使わない。

## exp192 Phase A review gate

- docs/experiment_plan_2026-06-10.md のreview gateに従って、直近Phase A risk probeをレビュー。
- 8 probes / 32 tasks のうち、7 groupはall-alive、1 groupのみtask187 partial-zero suspect。
- exp186 task187 repairはno gainだったため、successful repairは0。
- 判断: lower-ranked risk bisectionへ主予算を使い続けない。task187はfocused repair queueに残し、主PDCAはPhase C cost-band compressionへ移す。

## exp193/194 Phase C final output dtype probe

- task206/328について、最終FLOAT Castを外してBOOL outputでcostが下がるか検証。
- exp193: output名を変えたためruntimeが `Invalid output name:output` で失敗。
- exp194: output名を維持した版はfull validation passしたが、official costは task206 `61386 -> 61386`, task328 `68053 -> 68053` で改善なし。
- 判断: 最終Cast除去だけではscore-producingではない。Phase Cは内部full-grid中間のdtype縮小、またはfresh low-cost loweringへ進める。

## exp195/196 Phase C C-2 primitive miners

- exp195: `channel_gather_colormap` archetypeを全400 taskへ適用。mapping_ok_count `2`、improved_count `0`。提出なし。
- exp196: `static_slice_pad` の固定平行移動+zero padding版を全400 taskへ適用。shift_ok_count `1`、improved_count `0`。提出なし。
- 判断: 汎用の1-node color gather / fixed shift slice-padは現行artifactで概ね吸収済み。次は `one_node_conv_kernel` または解決済みruleのtask-specific loweringへ進む。

## exp197/198 Phase C one-node Conv miners

- exp197: gain_to_600上位taskに1x1/3x3 Conv線形fitを試したが、pixel-loop実装が重くtimeout。残存processを停止し、bounded retryへ縮小したがなお重かったため、汎用回帰版はいったん停止。
- exp198: 1x1 Convの特殊形として非injective color mapを高速scan。mapping_ok_count `4`、improved_count `0`。提出なし。
- 判断: 汎用per-color Conv横展開は改善なし。次はtask-specific solved-rule lowering、またはvectorized 3x3 Conv minerを別実験として設計する。
- 直近のrisk-inventory probeはall-aliveが続いているため、docs planのreview gateに従い、Phase A bisection継続の限界とPhase C cost-band compressionへの移行を検討する。

## exp199 task085 parity lowering audit

- task085の解決済みrelative parity ruleについて、固定global checkerboard parityで代替できるか検証。
- Python relative ruleは `265/265` pass。
- global parityはphase 0で `33/265`、phase 1で `40/265` のみpass。
- bar左端parityは `{0:435, 1:375}` と混在しており、幅も多様。
- 判断: static parity maskでは不十分。ONNX化するならrun-left parity検出、または別のrelative-position表現が必要。

## exp200/201 task185 axis-separable selector

- exp117ではtask185のpairwise window scoring proxyがcost `76194` でbaseline `59584` を超え、dynamic selectorは一度deprioritizeしていた。
- exp200でrow/col windowを独立にspecial-cell score最大で選ぶ監査を実施。
- 結果: pairwise reference `267/267`、axis-separable selectorも全mode `267/267`。row/col target rankは常に0。
- exp201でaxis selector cost proxyを計測。
- fused Conv proxyはcost `4156`、pairwise static proxy `76194` より大幅に安い。static Slice+ReduceSum proxyは `106064` で不採用。
- 判断: task185は再び有望。次はfused axis scoring + cheap spacing/core pathでcorrectness-first ONNX loweringを試す。

## exp202 task185 selected lattice extraction cost

- selected row/col indexが得られた後の4x4 lattice extraction + homogeneous 2x2 coreのcostを計測。
- `GatherElements` 2段 extraction + core + Pad: cost `7660`。
- 16-cell `GatherND` extraction + core + Pad: cost `38832`。
- exp201 axis selector proxy `4156` と足すと projected cost `11816`。baseline `59584` より十分低い。
- 推定local gainは `ln(59584/11816) ≈ +1.62` 点。
- 判断: task185は正しくdynamic indexを作れれば提出候補になりうる。次はall-pair列挙やstatic Slice分岐ではなく、axis selectorから小さいindex tensorを作るcorrectness-first candidateを試す。

## exp203 task185 dilated axis selector cost

- exp201のselector proxyは隣接raw row/col寄りだったため、spacing 3/4/5 の4-line windowをdilated Convでscoreする現実寄りproxyを計測。
- dilated axis scores only: cost `5208`。
- dilated axis selector + ArgMax: cost `5236`。
- exp202 extraction/core `7660` と合わせると projected cost `12868`。baseline `59584` より十分低い。
- 判断: selector realismでもcost laneは残る。次はdynamic index生成とbg/special-cell handlingを含むcorrectness-first ONNX candidateへ進む。

## exp204-206 task185 dynamic candidate debug

- exp204でdilated axis selector -> constant row/col index template -> `GatherElements` extraction -> homogeneous coreを接続した初回correctness-first candidateを作成。
- runtime/static checkは通ったが、全variantがexample 0でmismatchし `0_pass_1_fail`。
- costも `106720`〜`143490` とbaseline `59584` を超過。原因はfull `row_templates/col_templates` を巨大initializerとして持ったこと。
- exp205でPython側の任意dilated start selectorを監査。valid selector `267/267`、arbitrary selectorも `267/267` で完全一致したため、exp204 mismatchは任意start選択そのものではない。
- exp206でexample 0 tensor diffを診断。basicはchannel0背景だけを出し、selector scoreをcolor0除外したvariantは一部color1を出すが位置がズレる。default background補完でも不一致。
- 判断: task185はまだ有望だが、次は巨大templateをやめてcompact start/spacing tableからindexを導出すること、さらにselector indexと3x3 output位置の対応をdebugすることが必要。提出なし。

## exp207/208 task185 intermediate/bg diagnostics

- exp207でexp204 modelに中間outputを追加し、example 0の`row_idx`/`col_idx`/`lattice_4x4`を確認。
- Python selectorは `rr=[5,8,11,14]`, `cc=[5,8,11,14]`, bg `4`。
- ONNX basicは `[0,3,6,9]`、color0除外score variantsは `[2,5,8,11]` を選んでおり、Python selectorと不一致。
- 原因はselector scoreが「非背景」ではなく「非color0」になっていること。example 0の背景はcolor4なので、背景線がscoreに混入する。
- exp208でbg分布を確認したところ、task185の背景色は1-9全てに分散: `{1:32, 2:23, 3:30, 4:25, 5:26, 6:26, 7:34, 8:37, 9:34}`。
- 判断: 固定bg maskは使えない。task185を続けるならdynamic bg detectorが必要。cost template削減も未解決なので、次のreviewで別C-3 taskへpivotする選択肢を強く検討する。

## exp209-214 C-3 pivot: task365/task346

- task185はdynamic bg detectorが必要になったため、別C-3候補へpivot。
- exp209でtask365 selected bbox分布を確認。rule自体はfail `0` だが、selected bboxは `186` unique、object signatureは `266` unique。branch-table loweringは過学習リスクが高く、dynamic object selectionが必要。
- exp210でtask355 1x1 outputの単純aggregate ruleを探索。bestはcorner_br `70/267` で弱い。
- exp211で1x1 cropish候補 `355/346/291/048` に同じaggregate sweepを横展開。full hitはないが、task346の `least_nz` が `263/267` と近い。
- exp212でtask346の4 failを監査。正解は常にcount rank1で、rank0/rank1切替問題と分かった。
- exp213でrank-switch特徴を監査。`rank0_largest_component >= 8` が4 switchをTP4/FP0でカバー。
- exp214で補正规則をfull validation: 「非zero色のcount最少色を選ぶ。ただしその色の最大4-connected componentが8以上ならcount2位を選ぶ」で `267/267`。
- 判断: task346は新しいsolved-rule asset。ただしONNX化には色countとcomponent-size proxyが必要で即提出ではない。次はcount-only cost floor / component proxy costを測るか、より低costに出せる別taskを探す。

## exp215 task346 count/component cost probe

- task346 ruleのONNX cost floorを測定。
- 1x1 output floor: cost `59`。
- color count + ArgMin proxy: cost `111`。
- component growth proxy: 1 stepでも cost `117135`、4 step `468135`、8 step `936135`。
- baseline costは `9178`。
- 判断: count部分は非常に安いが、component-size補正が標準full-grid proxyでは重すぎる。task346はsolved-rule assetとして保持し、直接loweringは保留。次はcomponent不要のcount-only小出力taskか、別の低cost ruleを探す。

## exp216/217 1x1 cropish wide sweep

- exp087の1x1 cropish候補全体 `48/56/103/291/346/355` へ、mode/least/corner/bbox/edge/interior集約ruleを横展開。
- full hitはなし。
- task346は `interior_least_nz` で `265/267` まで改善。
- exp217で残り2 failを監査したが、simple count/edge/corner系でTP2/FP0の分岐は見つからず、component-like conditionがまだ必要。
- 判断: task346は保留継続。1x1単純集約laneは優先度を下げ、component-freeな3x3 crop/transformへpivot。

## exp218-220 3x3 cropish Slice/transform sweep

- exp218で3x3固定cropish 24 taskへ、固定位置/bbox位置の3x3 cropと回転/反転を横展開。
- full hit: task039 `bbox_top_left 3x3` (`264/264`)、task135 `top_right 3x3` (`266/266`)。
- task135はbaseline cost `360` で改善余地が小さいため、task039を優先。
- exp219でtask039 dynamic bbox cropを `GatherElements` でlowering。validationは `264_pass_0_fail` だがcost `48219`、baseline `7772` より悪い。
- exp220でdynamic `Slice` を試したが、static checkerが `dynamic shape crop` としてreject。
- 判断: task039 ruleは正しいが、現行loweringではscore-producingではない。dynamic bbox cropはstatic-shape-preservingな別表現が必要。

## exp221/222 fixed small-shape crop/colormap sweep

- exp221で固定小shape(area <= 12) cropish候補へ、固定anchor/bbox anchorのcrop + transformを横展開。
- full hitは task039、task135、task326。
- task039はbbox-onlyで、exp219/220のdynamic crop cost wallに該当。
- task135はfixed top-right 3x3だがbaseline cost `360`、task326はfixed/bbox top-left 2x2だがbaseline cost `160`。どちらも改善余地が小さい。
- exp222でglobal color-mapを重ねたが、新規有用hitはなく、full hitのmapはすべてidentity。
- 判断: fixed small-shape crop/colormap supplierは高cost taskに対する提出候補を出さなかった。次はmask/shape-family診断、またはtask-specific solved-rule loweringへpivot。

## exp223/224 small-output mask/shape inventory and task300 audit

- exp223で小出力cropish 58 taskのshape/mask familyを棚卸し。
- 高costのshape full-hitとして task300 baseline `77546`、task174 baseline `52019` を発見。
- exp224でtask300を個別監査。
- 結果: 全 `267/267` 例で、出力は最大sizeの4-connected same-color componentのbbox cropそのもの。`crop_exact=267`、`mask_colorized=267`、`rank_size_desc=267`。
- 判断: task300は新しいsolved-rule asset。baselineが大きいため有望だが、ONNX化には最大component選択とdynamic bbox cropが必要。次はcost proxyを測る。

## exp225-234 task300 max-color lowering and submission

- exp225でtask300は最大component growthを避けられると判明。最大nonzero色countが最大componentと `267/267` 一致し、4x3固定mask表現も `267/267`。
- exp226はONNX化初回で `0_pass_1_fail`。exp227でargmax gridは合うがone-hot背景が不足していると診断。
- exp228は背景channelを足したが、padding外側までchannel0=1にして失敗。exp229で888個の余分なchannel0を確認。
- exp230はpadding外側をzeroに戻しexample0は通ったが、可変bbox外側に背景を立ててexample1失敗。
- exp231でdynamic rectangular shape maskを追加し `267_pass_0_fail`、ただしcost `92595` でbaseline `77546` 超過。
- exp232でGatherElements対象を1ch spatial maskに縮小しcost `81541` まで改善したがまだno gain。
- exp233でselected color channelをGatherElementsで直接抜く構成にし、`267_pass_0_fail`、cost `52653`。task300単体で `+0.3871` local。
- exp234でexp178 current bestにtask300だけ差し替えたzipを作成し、Kaggle ref `53530035` として提出。expected public LB `6006.32`。
- 採点結果: public LB `6006.32`。期待 `6006.3171` と一致し、current public bestを更新。
- 採点待ち中にexp235でtask174を監査。最大色countは最大componentと `266/266` 一致するが、largest/max-color component cropは `138/266` のみ。task174はcomponent内部のsub-crop/shape ruleが必要。

## exp236/237 follow-up候補監査

- exp236でtask174の最大色component bbox/mask内部から、固定anchor/trim/flip/rotのsub-cropを探索。
- bestは引き続き `138/266` で、full/near hitなし。task174は単純な内部sub-cropではなく、より複雑な選択ruleが必要。
- exp237でtask253を監査。出力は4x4固定binary templateで、binary signatureは1種類。
- ただし出力色selectorは単純ではなく、best `mode_nz` / `least_nz` が `76/265`。static template loweringにはcolor selector ruleが未解決。
- 判断: task174/task253はいずれも保留し、次は別の高cost stable-binary/shape candidateを探索する。

## exp238 task130 component output audit

- exp223の次候補としてtask130を監査。
- 出力は全例3x3で、componentのbbox shapeとは `265/265` 一致する。
- ただし内容はcomponent crop/maskではほぼ説明できず、component exactishは `8/265` のみ。
- max-color crop/mask、largest component crop/maskはいずれも `0/265`。
- 同shapeの入力crop exactも `5/265` に留まる。
- 判断: task130はtask300型ではない。shape一致だけを根拠にloweringへ進むと失敗するため、cost probeせず別候補へpivotする。

## exp239-243 task271 crop/block selector

- exp239でtask271を監査。出力は3x3固定・2色・binary signature 1種類。
- component crop exactは `218/267` でfullではないが、入力内の3x3 exact cropは `267/267` 存在。
- exp240でexact crop offsetは各例一意。ただし単純selectorはbest `comp_area_desc 176/267`。
- exp241でbinary templateは3x3 all-nonzeroで、各入力に候補blockが常に4個あると判明。
- exp242で4候補のうち `color8_count_min` または `sum_colors_min` が正解blockを `267/267` 選択。task271 rule_found。
- exp243でONNX化。validationは `267_pass_0_fail` だが、cost `70847` vs baseline `28991` でno_cost_gain。
- 判断: task271はsolved-rule assetとして保持するが、Conv+ArgMin+GatherElements loweringは提出しない。より安い4-block専用selectorが見つかるまで別候補へpivot。

## exp244 task391 static template audit

- stable-binary候補task391を監査。
- 出力は3x1固定で、binary signatureは `(1,1,1)` の1種類。つまり3セル同色template。
- ただし出力色は1-9へ分散し、単純な入力特徴ではbest `bbox_bl 49/267`。
- 判断: templateは固定だがcolor selectorが未解決。static 3x1 loweringはしない。

## exp245-247 task100 static template / bbox-area selector

- task100をstable-binary候補として監査。
- exp245で出力は2x2全同色template、mode_nz色が `248/266` と近い。
- exp246で残り18例を監査し、出力色は「非zero色のうちbbox area最大の色」で `266/266` と判明。
- exp247でONNX化。validationは `266_pass_0_fail` だが、cost `74548` vs baseline `6536` でno_cost_gain。
- 判断: task100はsolved-rule assetとして保持。bbox-area selector loweringが高すぎるため提出しない。

## exp248/249 low-binary follow-up

- exp248でtask291を監査。1x1固定だが、count/bbox/component selectorは弱く、best `count_rank2 76/265`。
- exp249でtask274を監査。出力色は常に8だが、3x3 template 4種類のselectorが弱くbest `70/269`。
- 判断: 両方とも短期lowering候補ではない。固定色/単純selectorの別候補へpivot。

## exp250 task242 static template color audit

- task242は3x3固定・binary template 1種類・baseline `20119`。
- 出力templateは3x3 all-nonzeroで固定だが、色は1-9へ分散。
- count/bbox/component selectorを監査したがbest `count_max_low 64/266`。
- 判断: 色selectorが弱いため保留。固定template系だけで掘るより、既にruleが解けたtask100/271の低cost loweringや既存artifact surgeryへ視点を戻す。

## exp251 small-output prune probe

- task100/242/253/271 の既存artifactに対し、未使用initializer pruneを試した。
- removed_initializersは全taskで `0`。
- validationは全passだがcostはbaselineと同一で改善なし。
- なおtask100の既存artifact名は `symbolic_largest_bbox_color` であり、exp246で見つけたbbox_area最大色ruleは既にかなり安く実装済みと分かった。
- 判断: 単純pruneでは伸びない。small-output固定template探索は no-hit が続いたため、次は別レーンまたはより体系的なexisting-artifact surgeryへ切り替える。

## exp252/253 full-arc bypass micro delta

- exp252でsmall-output高cost候補に1-pass node bypass surgeryをfull-arc gate付きで試した。
- 517候補中、sample20 improvedは2件、full-arc selectedはtask153の1件。
- task153 `Reshape_node7_to_input0`: cost `11212 -> 10947`、local delta `+0.023919`。
- exp253でexp234 current best bundle上にtask153 bypassを載せた提出zipを作成。
- zip sanity OK: 400 files, sha256 `003d03f3aa73f462863f34f36c55dde1db4cbfe69db3d7b7086bc84340fe4621`。
- Kaggle ref `53531156` として提出。採点結果は public LB `6006.32` でexp234と同点。
- 判断: 微小local delta `+0.023919` はLB表示上観測できず、bestはexp234 `6006.32` のまま。採点待ち方針どおり、次のsurgery候補探索へ進む。

## exp254/255 top30 full-arc bypass bundle

- exp254でcurrent best exp234 bundleのtop30 cost taskに1-pass bypass surgeryを拡大。
- generated `1634`、sample20 improved `66`、full-arc selected `13`。
- selected tasks: `133, 204, 216, 54, 280, 205, 382, 239, 398, 275, 158, 234, 19`。
- combined local deltaは `+0.090241`。exp253単独よりLB表示差分が期待できるためbundle化へ進めた。
- exp255で13件をexp234へ再適用し、全件full-arc pass、zip sanity `400` files / names ok。
- Kaggle ref `53531405` として提出。public LB `6006.39` でCOMPLETEし、current public bestを更新。
- exp255の採点待ち中にAGENTS.mdへ追記した運用ルールどおり、次のsurgery探索・記録整備を進めた。

## exp256/257 rank31-80 full-arc bypass bundle

- exp256でcurrent cost rank31-80に同じ1-pass bypass sweepを拡大。
- generated `1834`、sample20 improved `59`、full-arc selected `16`。
- selected tasks: `383, 281, 264, 284, 370, 208, 25, 77, 255, 191, 379, 340, 51, 243, 209, 101`。
- combined local deltaは `+0.331473`。特にtask340 `Cast_node0_to_input0` が cost `70342 -> 52342`、local `+0.29557` と大きい。
- exp257で16件をexp255 current bestへstackし、全件full-arc pass、zip sanity `400` files / names ok。
- Kaggle ref `53531558` として提出。public LB `6006.72` でCOMPLETEし、expected `6006.7215` と一致。current public bestをexp257へ更新。

## exp258/259 rank81-140 full-arc bypass bundle

- exp258でexp257 current bestのcost rank81-140に同じ1-pass bypass sweepを拡大。
- generated `1980`、sample20 improved `77`、full-arc selected `23`。
- selected tasks: `233, 177, 118, 330, 97, 367, 89, 324, 182, 224, 325, 80, 14, 378, 184, 231, 252, 27, 65, 46, 368, 196, 288`。
- combined local deltaは `+0.219056`。
- exp259で23件をexp257 current bestへstackし、全件full-arc pass、zip sanity `400` files / names ok。
- Kaggle ref `53531729` として提出。public LB `6006.94` でCOMPLETEし、expected `6006.9391` と一致。current public bestをexp259へ更新。

## exp260/261 rank141-220 full-arc bypass bundle

- exp259の採点確定後、AGENTS.mdの「採点待ち中に止まらない」運用に従って、次windowのgraph surgeryを継続。
- exp260でexp259 current bestのcost rank141-220に1-pass bypass sweepを実施。
- generated `2529`、sample20 improved `117`、full-arc selected `27`。
- selected tasks: `392, 232, 178, 245, 343, 96, 55, 86, 310, 228, 289, 390, 163, 82, 287, 74, 308, 371, 256, 397, 168, 59, 341, 319, 119, 58, 374`。
- combined local deltaは `+0.840087`。大きい改善は task289 `24972 -> 20472`, task82 `23479 -> 20239`, task287 `22566 -> 20006`, task232 `29497 -> 25577`。
- exp261で27件をexp259 current bestへstackし、全件full-arc pass、zip sanity `400` files / names ok。
- Kaggle ref `53532418` として提出。public LB `6007.78` でCOMPLETEし、expected `6007.7801` と一致。current public bestをexp261へ更新。
- 判断: graph surgery laneはrank141-220でもまだ大きめのdeltaが残っている。ただしLB7000には桁不足なので、次はrank221+ sweep継続と、task185/task271等の低cost lowering再検討を並行候補にする。

## exp262/263 rank221-320 partial full-arc bypass bundle

- 新方針: 提出はlocal estimateを更新した時だけ行い、毎回の探索結果は提出しない。
- exp262でexp261 current bestのrank221-320に1-pass bypass sweepを実施。
- task048付近で評価がexit 1になったため、checkpoint方式に変更。`after_task273` 時点でpartial resultを保存。
- exp262 partial: generated `1831`, fullarc_selected `17`, selected tasks `30, 345, 124, 78, 153, 188, 329, 212, 50, 3, 254, 369, 180, 45, 248, 357, 273`, local_delta `+0.549899`。
- exp263でpartial selected 17件を再生成し、全件full-arc replay pass。zip sanityは `400` files / names ok / sha256 `56db2bf23e14c57a141cddaeb4797f03ef205355155dd2f1ae617ae5befac9d9`。
- expected public LB if calibrated: `6008.329899`。local estimateが更新されたため、ref `53532720` として提出。
- 採点結果: public LB `6008.30` でCOMPLETE。期待より `0.03` 低いが、exp261から `+0.52` 改善しcurrent public bestを更新。
- 判断: rank221-320にもまだsurgery deltaが残る。ただし評価が不安定なtaskがあるため、以後はcheckpoint/replay方式を標準にし、提出はbundle replayでlocal estimateが更新された時だけにする。

## exp264/265 rank321-400 full-arc bypass bundle

- exp264でexp263 current bestのcost rank321-400にcheckpoint/replay前提の1-pass bypass sweepを実施。
- generated `500`, sample20 improved `11`, fullarc_selected `8`。
- selected tasks: `299, 229, 235, 339, 129, 144, 318, 26`。
- combined local deltaは `+0.597660`。低cost側でもlog score上は大きな改善が残っていた。
- exp265で8件を再生成し、全件full-arc replay pass。zip sanityは `400` files / names ok / sha256 `0c56a04adf6ffd1d0a1ac557f69f3ad39592550c2d57e93501de725612840818`。
- expected public LB if calibrated: `6008.897660`。local estimateが更新されたため、ref `53532884` として提出。
- 採点結果: public LB `6008.90` でCOMPLETE。期待値と一致し、current public bestを更新。
- 判断: graph surgeryのrank window sweepは一巡した。次はroadmap Phase Bのdtype memory probeへ戻る。

## exp266 dtype memory probe

- Phase Bの前提確認として、同一構造で中間dtypeだけを変えた合成ONNXを作成。
- 初回はinput dtype/rank不一致で失敗。float 4D inputへ修正後、Cast/Identity/Add系は実行できたが `score_network` が `None` を返した。
- 判断: 単純Cast/Identity/Addだけではofficial scoreの計測対象にならない可能性がある。次は既存artifact rewrite型、またはConv/Whereを含むprobeで再測定する。

## exp267 farm output bytes cost proxy

- roadmap の「提出はlocal estimate更新時だけ」に合わせるため、farm tooling の cost proxy を1施策だけ修正。
- `cost_extractor.py` は従来、中間tensor bytesを足し、最終 `output` を除外していた。これを `params + output tensor bytes` の較正に合わせた。
- dtype probe結果: `uint8=9000`, `float32=36000`, `int64=72000`。uint8がint64の1/8になることを確認。
- `FarmRunner.run_smoke()` 成功。`experiments/exp267_farm_output_bytes_cost_proxy/result.json` を保存。
- 判断: tooling較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は `cost_proxy=36000` でも `250-600_plausible` になる band 判定を別の1施策で修正する。

## exp268 farm cost band threshold proxy

- exp267の残ギャップとして、低cost op 少数なら `cost_proxy=36000` でも `250-600_plausible` と表示される古い override を確認。
- `cost_extractor.py` の IR band 判定から `all low-cost ops and len <= 3` override を削除し、`cost_proxy <= 600` / `<= 2000` を優先する形へ揃えた。
- dtype band probe結果: `uint8=9000`, `float32=36000`, `int64=72000` はすべて `high_cost_probe_only`。
- `FarmRunner.run_smoke()` 成功。`smoke_channel_gather` / `smoke_slice_pad` は `cost_proxy=36000` で `high_cost_probe_only`。
- 判断: tooling band較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は `Where MAC=0` / `recolor_direct cost 44` と hard reject/primitive 表現の整合を実測可能なprobeで確認する。

## exp269 farm Where MAC=0 guardrail

- 既知較正では `Where` は MAC=0 なので、`Where over full-grid output` という理由で hard reject するのは過剰と判断。
- `cost_extractor.py` の `CONDITIONAL_HIGH_RISK_OPS` から `Where` だけを削除した。`ScatterND` / `GatherND` / `ScatterElements` は維持。
- `Where` + `ONE_NODE_DATA_MOVEMENT` + `uint8 output` は `hard_reject=false`, `cost_proxy=9000`。
- `Where` + `FULL_GRID_COMPOSITION` は `full_grid_composition` 理由で reject のまま。1施策として `Where` hard rejectだけを外した。
- 判断: tooling guardrail較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は `recolor_direct(cost 44)` と `recolor_cast(cost 140)` の差を farm IR に表現する。

## exp270 farm IR param count proxy

- farm IR path は `param_count=0` 固定だったため、`recolor_direct(cost 44)` と `recolor_cast(cost 140)` の差を local estimate に反映できなかった。
- `cost_extractor.py` で `IRNode.attrs["param_count"]` を `param_count_proxy` に加算し、`cost_proxy = params + output bytes` にした。
- recolor probe結果: `recolor_direct` は `param_count=44`, output bytes `1`, `cost_proxy=45`。`recolor_cast` は `param_count=140`, output bytes `1`, `cost_proxy=141`。
- `FarmRunner.run_smoke()` 成功。
- 判断: tooling param proxy較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は candidate ranking / supplier で direct recolor を優先できるよう接続する。

## exp271 farm bundle cost tiebreak

- exp270で計算できるようになった `recolor_direct` / `recolor_cast` のcost差を、bundle candidateの採用順へ反映。
- `BundleLedger.accepted_candidates()` を `local_delta` 降順、同点なら `candidate_cost` 昇順で sort するよう変更した。
- probeでは、入力順が `recolor_cast(cost 141)` -> `recolor_direct(cost 45)` でも、返却順は `recolor_direct` -> `recolor_cast`。
- `FarmRunner.run_smoke()` 成功。
- 判断: tooling ranking較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は同一taskに複数accepted候補がある場合のbest 1件選択を検討する。

## exp272 farm best candidate by task

- exp271の残課題として、accepted候補が同一taskに複数あると、そのまま複数返る問題を確認。
- `BundleLedger.best_candidates_by_task()` を追加し、delta/cost-aware排序済み accepted からtaskごとに最初の1件だけを保持するようにした。
- probeでは accepted `3` 件から best-by-task `2` 件へ縮約。`task_recolor` は `recolor_direct(cost 45)` が残り、`recolor_cast(cost 141)` は除外された。
- `FarmRunner.run_smoke()` 成功。
- 判断: tooling dedupe較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は farm smoke/result に best-by-task count を標準表示する。

## exp273 farm bundle smoke best-by-task visibility

- exp272で追加した `best_candidates_by_task()` を farm smoke/result で見えるようにした。
- `farm_runner.py` の `bundle_smoke` に `best_by_task_count` と `duplicate_task_count` を追加。
- exp273 smoke結果は `accepted_count=1`, `best_by_task_count=1`, `duplicate_task_count=0`。
- 判断: tooling可視化のみで候補bundleのlocal estimate改善ではないため提出なし。次は実bundle構築入口で `best_candidates_by_task()` を使う接続を確認する。

## exp274 farm best total delta

- exp272/273でtask重複を見えるようにしたが、`total_local_delta()` はaccepted全件合計のままで、同一task候補を二重計上し得る。
- `bundle_manager.py` に `best_total_local_delta()` を追加し、best-by-task後の候補だけでdelta合計を出せるようにした。
- duplicate probeでは accepted `3`, best-by-task `2`, total delta `1.4`, best total delta `0.9`。
- `FarmRunner.run_smoke()` 成功。
- 判断: tooling accounting較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は farm smoke/result に `best_total_local_delta` を標準表示する。

## exp275 farm best total delta visibility

- exp274で追加した `best_total_local_delta()` を farm smoke/result に標準表示した。
- `farm_runner.py` の `bundle_smoke` に `best_total_local_delta` を追加。
- exp275 smoke結果は `accepted_count=1`, `best_by_task_count=1`, `duplicate_task_count=0`, `total_local_delta=0.1`, `best_total_local_delta=0.1`。
- 判断: tooling可視化のみで候補bundleのlocal estimate改善ではないため提出なし。次は `FarmRunner.write_notes()` の古い説明を現在のpolicyへ合わせる。

## exp276 farm write_notes policy refresh

- `FarmRunner.write_notes()` の文面が古く、`Where` rejectやaccepted countだけの説明に寄っていたため、現在のfarm policyに合わせた。
- notes templateに `params + output tensor bytes`, `Where MAC=0`, `FULL_GRID_COMPOSITION` / `ScatterND` は別guardrail、`best_by_task_count`, `duplicate_task_count`, `best_total_local_delta` を明記。
- `FarmRunner.run_smoke()` + `write_notes()` 成功。generated notesで `submit-gate local delta uses best-total value: 0.1` を確認。
- 判断: notes template較正のみで候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成側へ戻る。

## exp277 farm submission decision helper

- 提出ポリシーを各実験の手作業判断にせず、ledger側で `best_total_local_delta` ベースに機械化するため `submission_decision()` を追加。
- submitted best `6008.90` に対し、delta `0.2` のaccepted候補では `candidate_estimate=6009.10`, `should_submit=true`。
- 精度gate落ち候補だけの場合は `best_total_local_delta=0`, `candidate_estimate=6008.90`, `should_submit=false`。
- `FarmRunner.run_smoke()` 成功。
- 判断: tooling helper追加のみで実候補bundleのlocal estimate改善ではないため提出なし。次は farm smoke/result に submission decision を標準表示する。

## exp278 farm submission decision visibility

- exp277で追加した提出可否helperを farm smoke/result に接続した。
- `FarmConfig.submitted_best_estimate` を追加し、既定値は exp265 current best `6008.90`。
- `bundle_smoke.submission_decision` に `submitted_best_estimate`, `best_total_local_delta`, `candidate_estimate`, `should_submit` を表示。
- smoke dummy ledgerでは `best_total_local_delta=0.1`, `candidate_estimate=6009.0`, `should_submit=true`。
- 判断: smoke dummyに対する表示確認であり、実候補bundleのlocal estimate更新ではないため提出なし。次は smoke-only decision reason を明示し、実提出判断との混同を防ぐ。

## exp279 farm smoke submission scope

- exp278の残課題として、smoke dummy ledgerの `should_submit=true` が実提出判断と混同されるリスクを確認。
- `farm_runner.py` の `bundle_smoke` に `submission_decision_scope` と `submission_decision_reason` を追加。
- exp279 smoke結果は `scope=smoke_dummy_not_kaggle_candidate`、reason は `run_smoke uses synthetic ledger rows only; do not submit from this decision`。
- 判断: tooling誤読防止のみで候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp280 farm write_notes submission scope

- exp279でresultにはsmoke-only scope/reasonを出したが、notes templateには出ていなかった。
- `FarmRunner.write_notes()` に `submission decision scope` と `submission decision reason` を追加。
- generated notesで `smoke_dummy_not_kaggle_candidate` と `run_smoke uses synthetic ledger rows only; do not submit from this decision` を確認。
- 判断: notes可視化のみで候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp281 farm write_notes submission decision values

- `FarmRunner.write_notes()` に submission decision の数値本体が出ていなかったため、`submitted_best_estimate`, `candidate_estimate`, `should_submit` を追加。
- generated notesで `submitted best estimate: 6008.9`, `candidate estimate: 6009.0`, `should submit: True` を確認。
- 判断: smoke dummy notesの可視化のみで実候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp282 farm IR band uses cost proxy

- exp270でIRの `param_count` は `cost_proxy` に入るようになったが、band判定は `memory_bytes_proxy` だけを見ていた。
- `cost_extractor.py` の `assess_ir()` で `cost_proxy = params + output bytes` を明示し、band判定を `cost_proxy` 基準へ変更。
- probe結果: `param_count=44` + output 1 byte は `cost_proxy=45`, `250-600_plausible`。`param_count=5000` + output 1 byte は `cost_proxy=5001`, `high_cost_probe_only`。
- 判断: tooling band較正のみで実候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp283 farm recolor primitives

- exp270でrecolor direct/castのcost差は表現できたが、IR primitive kindとしては区別できなかった。
- `ir.py` の `PrimitiveKind` に `RECOLOR_DIRECT` と `RECOLOR_CAST` を追加。
- probe結果: directは primitive kind `recolor_direct`, `cost_proxy=45`。castは `recolor_cast`, `cost_proxy=141`。
- 判断: IR表現追加のみで実候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp284 farm current best public LB visibility

- farm の `PublicCodeRegistry.floor_status()` が public-code sourceだけを見ており、現行Best Public LB `6008.90` が表示されないリスクを確認。
- `public_code.py` で `EXP_SUMMARY.md` の `Best Public LB` 行を読み、`current_best_public_lb` と `max_observed_kaggle_lb` に反映するよう変更。
- smoke結果: `current_best_public_lb=6008.9`, `max_observed_kaggle_lb=6008.9`, `submit_floor_ready=false`。
- 判断: tooling visibilityのみで実候補bundleのlocal estimate改善ではないため提出なし。次は実候補生成へ戻る。

## exp285 farm recolor IR factories

- exp283で追加した `RECOLOR_DIRECT` / `RECOLOR_CAST` を supplier が使える形にするため、`ir.py` に標準factoryを追加。
- probe結果: direct `cost_proxy=45`, cast `cost_proxy=141`。
- 判断: direct-first の候補生成方針をIR層で表現できるようになった。実候補bundleのlocal estimate改善ではないため提出なし。

## exp286 farm recolor smoke visibility

- Plan: Phase C supplier復帰に向け、recolor direct/castのcost差をfarm smokeで常時確認できるようにする。
- Do: `farm_runner.py` の `_smoke_programs()` に `recolor_direct_program()` / `recolor_cast_program()` を追加。
- Check: direct `cost_proxy=45`, cast `cost_proxy=141`。`submission_decision_scope=smoke_dummy_not_kaggle_candidate`。
- Act: 実候補bundleのlocal estimate改善ではないため提出なし。次は実taskに対するsupplier接続へ進む。

## exp287 bundle computed local delta

- Plan: 実候補ledger化に向け、提出判断のlocal estimateを手入力ではなくcost比から計算する。
- Do: `bundle_manager.py` に `computed_local_delta=ln(base_cost/candidate_cost)` を追加し、ranking/total/best_total/submission decisionを接続。
- Check: synthetic `1000 -> 500` は `+0.693147`。validation fail candidate は accepted されない。farm smokeでも `candidate_estimate=6009.59314718056`。
- Act: synthetic ledgerのみで実候補bundleのlocal estimate改善ではないため提出なし。

## exp288 bundle CSV computed delta

- Plan: real candidate reviewで見るCSVにも、提出判定と同じcost-derived deltaを出す。
- Do: `bundle_manager.py` の `write_csv()` に `computed_local_delta` 列を追加。
- Check: smoke CSV first rowは `computed_local_delta=0.6931471805599453`。`best_total_local_delta` と一致。
- Act: smoke visibilityのみで実候補bundleのlocal estimate改善ではないため提出なし。

## exp289 bundle selected manifest ingestion

- Plan: real candidate manifestを `BundleLedger` に直接取り込み、cost-derived submit decisionへ接続する。
- Do: `bundle_manager.py` に `BundleLedger.from_selected_manifest()` を追加。`full_arc_pass` は `266_pass_0_fail` に正規化。
- Check: exp264 selected manifest 8件をacceptedし、`best_total_local_delta=0.5976596441240898` を再現。
- Act: exp264/exp265 deltaは既に現行bestに含まれるため提出なし。次は未提出の新規manifestに同じ入口を使う。

## exp290 bundle multi manifest ingestion

- Plan: 複数selected manifestを1つのledgerに取り込み、bundle reviewできるようにする。
- Do: `bundle_manager.py` に `BundleLedger.from_selected_manifests()` を追加。
- Check: exp260+exp264 manifestで `candidate_count=35`, `accepted_count=35`, `best_total_local_delta=1.4377463495672809`。重複taskは0。
- Act: 履歴manifestで現行best lineageに含まれるため提出なし。次は未提出manifestへ適用する。

## exp291 bundle exclude submitted sources

- Plan: 現行best lineageの履歴manifestを再提出候補として誤読しないよう、source除外を提出判定前に入れる。
- Do: `bundle_manager.py` の `BundleLedger.from_selected_manifests()` に `exclude_sources` を追加。
- Check: exp260+exp264は除外なしで `should_submit=true`、同source除外後は `candidate_count=0`, `should_submit=false`。
- Act: fresh local estimate改善なしのため提出なし。

## exp292 submission decision counts

- Plan: `submission_decision` だけでfresh候補件数を監査できるようにする。
- Do: `bundle_manager.py` の `submission_decision()` に `accepted_count` と `best_by_task_count` を追加。
- Check: 最近のbypass manifest全体では `accepted_count=104`, `best_total_local_delta=2.6284155220455716`。現行best lineage除外後は `accepted_count=0`, `should_submit=false`。
- Act: fresh local estimate改善なしのため提出なし。次は候補生成側へ戻る。

## exp293 bundle fresh submission decision

- Plan: real candidate manifest生成後に、現行best lineage除外と提出判定を1回で行うhelperを作る。
- Do: `bundle_manager.py` に `BundleLedger.fresh_submission_decision()` を追加。
- Check: 最近のbypass manifest全体は `should_submit=true` だが、fresh-only判定では `accepted_count=0`, `should_submit=false`。
- Act: fresh local estimate改善なしのため提出なし。次はscore-producing候補生成へ戻る。

## exp294 bundle full_arc_pass gate

- Plan: real supplier が `full_arc_pass` 表記の候補を直接 `BundleCandidate` に入れても accuracy gate を通れるようにする。
- Do: `bundle_manager.py` の `BundleCandidate.accepted` で `full_arc_pass` と `*_pass_0_fail` を pass 扱いにした。
- Check: synthetic probeで `full_arc_pass` と `266_pass_0_fail` はaccepted、`24_pass_1_fail` はrejected。
- Act: synthetic gate probeのみで実候補bundleのlocal estimate改善ではないため提出なし。

## exp295 exp262 resume attempt

- Plan: toolingではなくscore-producing候補生成へ戻り、exp262 partial (`after_task273`) の先に追加selectedがあるか確認する。
- Do: 既存 `run_exp262.py` を再実行。
- Check: runは exit code `1`。checkpoint は再び `after_task273`、17 selected、`local_delta=0.5498990065818337`。task048付近でONNXRuntime reshape/conv runtime error。
- Act: fresh delta は0で、現行bestを更新しないため提出なし。次はtask048 bad candidatesをskip/quarantineして残りwindowを完走する。

## exp296 bundle exclude tasks quarantine

- Plan: task048のようなruntime-stopperをfresh submit reviewから除外できるようにする。
- Do: `bundle_manager.py` の multi-manifest/fresh submission decision に `exclude_tasks` を追加。
- Check: synthetic task048/task049 manifestで task048 quarantine は1候補だけ残り、全task quarantineでは `accepted_count=0`, `should_submit=false`。
- Act: synthetic probeのみで実候補bundleのlocal estimate改善ではないため提出なし。生成側のskipは次課題。

## exp297 exp262 skip task048/336 fresh candidates

- Plan: exp262 rank221-320 sweepを、runtime-stopperの task048/task336 を skip して完走し、exp263 partialで提出済みの17件を除いた fresh 改善だけを確認する。
- Do: `e297_short_skip048_336` で skip 実行を完走。20件 selected のうち17件は exp263 partial と重複、fresh は task060/task226/task123 の3件。これを `exp297_exp262_skip_task048_336_fresh_candidates` で exp265 current best bundle へ replay。
- Check: 3件とも full-arc replay pass。local delta `+0.05520869941921447`、expected public LB `6008.955208699419`、zip sanity 400 files / names OK。
- Act: 現行 best `6008.90` を local estimate で上回ったため Kaggle ref `53535771` として提出。採点結果は public LB `6008.96` で COMPLETE。期待 `6008.955208699419` と一致し、current best を更新。

## exp298 bypass sweep yield analysis

- Plan: exp297 採点待ち中に、exp254以降の full-arc bypass sweep の収穫率を集計し、次に同じ lane を続けるべきか判断する。
- Do: exp254/256/258/260/262_partial/264/297_fresh の selected manifests を集計。
- Check: 107 unique tasks、total local delta `+2.683624221464793`。exp264 rank321-400 は8件で `+0.597660` と強かったが、exp297 fresh remainder は3件 `+0.055209` まで低下。
- Act: bypass lane は換金済みで有効だが、現 one-pass supplier の辺際収益は低下。exp297 採点後は task048/task336 の局所修復か、roadmap Phase C の fused low-cost lowering / solved-rule 換金へ戻す。

## exp299 farm submitted best 6008.96

- Plan: exp297 が current best `6008.96` になったため、farm の提出判定基準を古い `6008.90` から更新する。
- Do: `experiments/neurogolf_farm/farm_runner.py` の `FarmConfig.submitted_best_estimate` を `6008.96` に変更。
- Check: `FarmRunner.run_smoke()` を `experiments/exp299_farm_submitted_best_6008_96/` に出力し、submission decision の `submitted_best_estimate=6008.96` を確認。
- Act: tooling基準更新のみ。smoke dummy は Kaggle candidate ではないため提出なし。次の score-producing loop は exp298 の分析通り、task048/task336 局所修復か Phase C fused low-cost lowering へ進める。

## exp300 farm GridSample low cost gate

- Plan: roadmap Phase C の低cost fused loweringへ戻る。`one_node_gridsample` 実例を supplier 化する前に、farm cost gate が `GridSample` を low-cost op として扱えるようにする。見込みは `param_count=8` + `uint8` output 1 byte = `cost_proxy=9`。
- Do: `cost_extractor.py` の `LOW_COST_OPS` に `GridSample` を追加。
- Check: synthetic IR `smoke_one_node_gridsample` は `cost_proxy=9`, `predicted_cost_band=250-600_plausible`, `hard_reject=false`。
- Act: tooling gate の更新のみで実候補bundleのlocal estimate改善ではないため提出なし。次は GridSample supplier/逆設計候補を実taskへ接続する。

## exp301 farm GridSample IR factory

- Plan: exp300で `GridSample` が cost gate を通るようになったため、Phase C supplier が使える標準IR factoryを追加する。見込みは `param_count=8` + `uint8` output bytes。
- Do: `ir.py` に `gridsample_program()` を追加。
- Check: factory経由の synthetic IR は `cost_proxy=9`, `predicted_cost_band=250-600_plausible`, `hard_reject=false`。
- Act: tooling factory の追加のみで実候補bundleのlocal estimate改善ではないため提出なし。次は `farm_runner.py` smoke visibility か実task supplier接続へ進む。

## exp302 farm GridSample smoke visibility

- Plan: exp301で追加した `gridsample_program()` を farm smoke に接続し、Phase C supplier の低cost lane を毎回見えるようにする。見込みは `param_count=8` + `uint8` output 1 byte = `cost_proxy=9`。
- Do: `farm_runner.py` の `_smoke_programs()` に `smoke_gridsample` を追加。
- Check: farm smoke で `smoke_gridsample` は `cost_proxy=9`, `predicted_cost_band=250-600_plausible`, `hard_reject=false`。
- Act: smoke visibility のみで実候補bundleのlocal estimate改善ではないため提出なし。次は GridSample supplier/逆設計候補を実taskへ接続する。

## exp303 farm GridSample primitive kind

- Plan: Phase C の `one_node_gridsample` lane を generic one-node data movement と切り分け、supplier/ledger分析で混ざらないようにする。見込みコスト削減は直接なし。
- Do: `ir.py` に `PrimitiveKind.GRID_SAMPLE` を追加し、`gridsample_program()` の node kind を差し替え。
- Check: factory出力は `primitive_kinds=("grid_sample",)`, `cost_proxy=9`, `predicted_cost_band=250-600_plausible`, `hard_reject=false`。
- Act: tooling label の更新のみで実候補bundleのlocal estimate改善ではないため提出なし。次は実task supplier接続へ進む。
