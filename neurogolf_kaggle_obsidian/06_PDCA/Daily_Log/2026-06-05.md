# 2026-06-05

## exp010-exp012 PDCA

### Plan

- CUDA/PyTorch導入を先に行い、GPUをroute分類とtemplate候補rankingに使う。
- 公式提出候補は引き続きstatic ONNX、禁止opなし、official-like cost、sample validationで採用する。
- 6500未達の場合は、public artifact追加、template factory、task別graph surgeryの順にPDCAを回す。

### Do

- `exp010_cuda_gpu_setup` で `.venv` に `torch==2.12.0+cu126` と `torchvision==0.27.0+cu126` を導入。
- RTX 2080 SUPERで CUDA smoke test、small tensor matmul、small CNN forward/backward を確認。
- `exp011_gpu_route_classifier` で軽量CNN + tabular特徴をGPU学習。
- `exp012_template_factory_core` で `signature_scatter_lookup`、固定幾何、固定mask、色写像、crop templateを実装。
- Kaggle APIで追加公開dataset/kernel一覧を確認し、exp008時点で多くが既にregistryに入っていることを確認。

### Check

- exp010: CUDA available、device `NVIDIA GeForce RTX 2080 SUPER`、torch CUDA `12.6`。
- exp011: valid accuracy `0.9487`、macro F1 `0.8551`。
- exp012: sample-local estimate `6296.2974`、baseline `6282.2302`、delta `+14.0671`。
- exp012: improved task `38`、adopted templateは `signature_scatter_lookup`。
- exp012: `submission.zip` は400ファイル、`taskNNN.onnx` のみ。
- 6500には未達。lookup系はarc-gen labelを使うためleakage/overfitting riskが高い。

### Act

- exp012は提出候補ではなく、local上限探索として扱う。
- 次はtop cost taskごとのgraph surgeryと、GPUで学習した小型CNN/conv templateのstrict採用を試す。
- private-like validationを作り、public/local寄せの危険度を分離して評価する。

## exp002 PDCA

### Plan

- `exp002_public_blend_6500_fast` として、公開artifact/sourceを収集し、strict static validationとofficial-like costでtask別blendを作る。
- 目標はKaggle submitではなく、local推定6500台の `submission.zip` を作ること。

### Do

- `experiments/exp002_public_blend_6500_fast/build_public_blend.py` を実装。
- dataset/kernel outputを `data/cache/public_artifacts/` に取得。
- `massimilianoghiotto/neurogolf2026-6254` をbaseに、追加dataset/kernel outputをPDCAで拡張。
- score cacheを導入し、新規sourceだけを再評価できるようにした。

### Check

- 最終local estimate: `6282.075718068686`
- selected: `400 / 400`
- selected validation: `400 pass`
- `submission.zip` は `task001.onnx` ... `task400.onnx` の400ファイルのみ。
- 6500未達。主なrejectは `Compress`, functions, dynamic shape, invalid dim。

### Act

- 次はofficial rulesで `Compress` / functions / dynamic shapeの扱いを再確認する。
- strict filter維持なら、公開artifact追加だけでなくtop expensive tasksの自前rewriteが必要。

## Plan

- Create clean experiment workspace.
- Prepare data download and experiment tracking.
- exp001_baselineとして、初回提出計画に従いローカルONNX検証環境を作る。

## Do

- `docs/neurogolf_first_submission_plan.md` を確認。
- `nvidia-smi` でGPU driverを確認。RTX 2080系 8GB VRAM、CUDA driver 13.1。
- `.venv` を作成し、`kaggle`, `onnx`, `onnxruntime`, `onnx-tool`, `numpy` を導入。
- `onnxruntime` の `CPUExecutionProvider` が使えることを確認。
- Kaggle APIでコンペファイル一覧取得を試したが、未認証で停止。
- Kaggle API認証後、公式Evaluation/Data/Constraints/May 4 updateを確認。
- `data/raw` は触らず、`data/external/neurogolf-2026` にコンペデータを取得・展開。
- `task087` を最小taskとして選び、3x3 180度回転ONNXを手書き実装。
- 公開例で ARC-AGI `5/5`、ARC-GEN `261/261` pass。
- `submission.zip` を作成。Kaggle submitは未実施。
- Kaggle APIで `submission.zip` を提出。ref `53383536`。
- submission statusは `COMPLETE`、publicScoreは `14.50`。

## Check

- 初回計画はGPU学習ではなく、CPUでONNXグラフの正しさとコストを検証する内容。
- `data/raw` は `.gitkeep` のみ。コンペデータは `data/external/neurogolf-2026` に取得済み。
- GPU driverは見えているが、`.venv` のPyTorch CUDA環境は未構築。
- 公式costは `params + memory bytes`。MACsは2026-05-04 updateでobjectiveから除外。
- task087 ONNXはmemory `36000`、params `60`、推定 `14.507` points。
- LB `14.50` はローカル推定 `14.507` と概ね一致。
- private benchmarkで同じ3x3 rot180規則が成立する保証はない。

## Act

- 次は同様の単純変換taskを増やすか、task087のcost削減を試す。
